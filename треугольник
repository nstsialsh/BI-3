def is_inside_simple(A, B, C, P):
    x1, y1 = A     # A - это кортеж (x1, y1), например (0, 0)
    x2, y2 = B     # B - это кортеж (x2, y2), например (4, 0) 
    x3, y3 = C     # C - это кортеж (x3, y3), например (0, 3)
    x, y = P
